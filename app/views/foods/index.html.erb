<section class='m-4 p-6'>
  <h3>All Foods</h1>
  <div class="flex justify-content-end p-6 m-4">
<button type="button" class="btn btn-warning">
   <%= link_to 'Add new food', new_food_path %>
</button>
  </div>
  <table class="border border-warning p-6 m-4">
    <thead>
        <tr>
        <th scope="col" class="p-2">Food</th>
        <th scope="col" class="p-2">Measurement</th>
        <th scope="col" class="p-2">Unit Price</th>
        <th scope="col" class="p-2">Actions</th>
        </tr>
    </thead>
    <tbody>
      <% @foods.each do |food| %>
        <tr>
        <th scope="row"> <%= food.name %> </th>
        <td> <%= food.measurement_unit %> </td>
        <td>$<%= food.price %> </td>
        <td>
            <%= button_to "Delete", food_path(food.id ), :method => :delete, class: "btn btn-danger m-3" %>

        </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>